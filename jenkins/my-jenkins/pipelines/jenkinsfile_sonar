pipeline {
    agent any

    stages {
        stage('Find correct path'){
            echo "${currentBuild}"
            sh '''
             ls
            '''
        }

        // stage('Login Sonar'){
        //     withSonarQubeEnv('sonar'){

        //     }
        // }
    }
}