pipeline {
    agent any

    environment {
        APP_PATH = 'jenkins/Jenkins_Upgradev3'  // Changed from PATH to avoid conflicts
    }

    stages {
        stage('Find correct path') {
            steps {
                dir("./${APP_PATH}/java-tomcat-sample") {
                    sh "ls"  // Lists contents of the directory for verification
                }
            }
        }

        // Additional stages can be added here as needed.
    }
}
