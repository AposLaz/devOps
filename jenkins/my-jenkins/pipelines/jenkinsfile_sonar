pipeline {
    agent any

    stages {
        stage('Find correct path'){
            steps{
                echo "${currentBuild}"
                sh '''
                ls
                '''
            }
        }

        // stage('Login Sonar'){
        //     withSonarQubeEnv('sonar'){

        //     }
        // }
    }
}