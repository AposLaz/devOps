pipeline {
    agent any

    environment {
        PATH = 'jenkins/Jenkins_Upgradev3'
    }
    stages {
        stage('Find correct path'){
            steps{
                echo "${PATH}"
            }
        }

        // stage('Login Sonar'){
        //     withSonarQubeEnv('sonar'){

        //     }
        // }
    }
}