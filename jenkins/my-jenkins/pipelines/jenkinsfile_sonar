pipeline {
    agent any

    environment {
        PATH = 'jenkins/Jenkins_Upgradev3'
    }
    stages {
        stage('Find correct path'){
            steps{
                dir("./${PATH}/java-tomcat-sample"){
                    sh 'ls'
                }            
            }
        }

        // stage('Login Sonar'){
        //     withSonarQubeEnv('sonar'){

        //     }
        // }
    }
}